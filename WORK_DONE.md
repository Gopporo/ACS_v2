## Что было сделано

Я проверил проект и довел его до рабочего состояния.

### 1) Исправил падение сборки
- Была ошибка в тесте `AdminControllerTests`: тест ожидал `boolean`, хотя метод в сервисе уже стал `void`.
- Обновил тест под текущую логику контроллера и сервисов.
- После этого проект снова начал собираться и тесты проходят.

### 2) Доработал тесты под новую архитектуру
- Добавил недостающие моки для зависимостей `AdminController`.
- Обновил проверки в тестах: теперь тестируется вызов `ModelAttributeHelper`, как в реальном коде.
- Убрал устаревшую инициализацию моков через конструктор, перевел на актуальный стиль с `MockitoExtension`.

### 3) Усилил обработку ошибок в сервисах
- `CustomUserDetailsService`: теперь при отсутствии пользователя выбрасывается `UsernameNotFoundException` (раньше мог вернуться `null`).
- `ReportService`: заменил общие `RuntimeException` на `ResourceNotFoundException` для понятных ошибок по доменной модели.
- `DepartmentService`: аналогично заменил общие исключения на `ResourceNotFoundException`.
- `ZoneService`: метод получения зоны по ID теперь тоже выбрасывает `ResourceNotFoundException`, если зона не найдена.

### 4) Убрал часть "незавершенности" в контроллере директора
- В `DirectorController` заменил магическое число `-1` на константу `ALL_LEVELS`.
- Это делает код понятнее и снижает риск ошибок при дальнейших изменениях.

### 5) Проверил качество после доработок
- Запустил `mvn test` после изменений.
- Тесты проходят успешно.
- Проверил линтер: новых ошибок нет.

### 6) Добавил глобальную обработку ошибок
- Добавил `@ControllerAdvice` и показ “правильных” страниц (`error`, `403`, `registration`).
- Добавил отсутствующий шаблон `error.ftlh`, чтобы ошибки не ломали рендеринг.

### 7) Привел конфигурацию в порядок
- Упростил `SecurityConfig`: убрал `web.ignoring(...)` и явно разрешил статику через `permitAll`.
- Отключил лишний SQL/TRACE лог и включил `spring.jpa.open-in-view=false`.
- Привел `ModelAttributeHelper` к использованию констант для ключей `errorMessage/successMessage`.

### 8) Добавил валидацию DTO и тесты
- Подключил Bean Validation и сделал валидацию для `UserRegistrationDto` и `UserUpdateDto` через `@Valid`.
- Добавил unit-тесты на `GlobalExceptionHandler`, валидацию DTO и `ModelAttributeHelper`.

## Итог простыми словами

Проект снова стабильно собирается, тесты работают, а обработка ошибок стала более аккуратной и предсказуемой.
Код стал немного чище и ближе к единому стилю, чтобы его было проще дальше развивать.
